-- Core options & leader
require("config.options")

-- Plugins (lazy.nvim + all plugin configs)
require("config.plugins")

-- LSP setup (servers + keymaps + diagnostics + format)
require("config.lsp")
